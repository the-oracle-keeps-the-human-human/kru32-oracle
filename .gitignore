# ESPHome build artifacts
.esphome/
.pio/
build/

# Node
node_modules/

# Astro build output (CI rebuilds)
dist/
.astro/

# gen-capture scratch (dev-only preview regeneration)
_capture.html

# Secrets (never commit)
secrets.yaml

# OS
.DS_Store
# Learn: ignore origin symlinks (source lives in ghq)
ψ/learn/**/origin
